from setuptools import setup, find_packages

setup(
    name='ai-llmops-project',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Include your dependencies
    ],
)